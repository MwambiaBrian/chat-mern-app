import React from "react";
import ScrollableFeed from "react-scrollable-feed";

const ScrollableChat = ({ messages }) => {
  return <ScrollableFeed>ScrollableChat</ScrollableFeed>;
};

export default ScrollableChat;
